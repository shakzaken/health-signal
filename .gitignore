# Terraform
terraform/.terraform/
terraform/terraform.tfvars
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/.terraform.lock.hcl

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# uv
.uv/

# Environment variables
.env
.env.*
!.env.example

# Uploads / user data
uploads/

# Design docs
design/

# Local test data (not for version control)
test-data/

# IDE
.vscode/
.idea/
*.swp
*.swo

# macOS
.DS_Store
**/.DS_Store

# Claude Code local settings (personal, not project-wide)
.claude/settings.local.json

# Logs
*.log
logs/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Alembic (keep migrations, ignore compiled cache)
backend/alembic/versions/__pycache__/

# FastEmbed model cache (downloaded at runtime)
.fastembed_cache/

# Jupyter
.ipynb_checkpoints/

# Slides (presentations and generators)
docs/slides/
assets/social/health-signal-linkedin.png

# Local Docker overrides (dev port bindings — not for production)
docker-compose.override.yml

# Local maintenance scripts (not for version control)
scripts/

# Server runbook (contains IPs and private infrastructure details)
docs/server-runbook.md

# TLS certificates (never commit)
nginx/certs/*.pem
nginx/certs/*.key

# Eval generated outputs (results and reports are produced per run, not committed)
eval/tests/*/results.json
eval/tests/*/report.md
